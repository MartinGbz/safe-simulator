{
  "name": "safe-simulator",
  "version": "1.0.0",
  "description": "Simulate a Safe multisig transaction locally using Foundry — no Tenderly, no Safe UI required",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "viem": "^2.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
